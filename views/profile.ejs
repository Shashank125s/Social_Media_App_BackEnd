<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="w-full h-screen bg-zinc-900 px-3 py-5  text-white">
        <div class="flex">
            
            <div class="w-10  h-10 mt-5 ml-3  rounded-md overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilePic %>" alt="">
            </div>
        <h3 class="text-2xl ml-2 px-3 py-5"><span class="font-light">Hello</span>, <%= user.name %> 👋</h3>
        
        </div>
        <a href="/logout" class="bg-red-600 rounded-md inline-block ml-7 px-3 py-2">Logout</a>

        <div class="px-3 py-5 ml-5">
            <h5 class="mb-5">You can create a new post</h5>
            <form method="post" action="/post">
                <textarea placeholder="What's on your mind?" class="p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-600 rounded-md" name="content"></textarea>
                <input class="px-3 py-2 bg-blue-600 block rounded-md mt-2 text-sm" type="submit" value="Create New Post">
            </form>
        </div>
        <div class="post mt-20 ml-7"> 
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer">

                <% user.posts.reverse().forEach(function(post) { %>
                    <div class="post w-1/3 mb-3 p-4 border-[1px] rounded-md border-zinc-800 bg-zinc-800">
                        <h4 class="text-blue-400 mb-3">@<%= user.username %></h4>
                        <p><%= post.content %></p>
                        <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                        <div class="btn flex gap-4 mt-2">
                            <a class="text-blue-400" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-600" href="/edit/<%= post._id%>">edit</a>
                        </div>
                    </div>
                <% }) %>

            </div>
        </div>
    </div>
    
</body>
</html>