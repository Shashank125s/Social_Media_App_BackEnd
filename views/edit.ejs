<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="w-full h-screen bg-zinc-900 px-3 py-5 ">
        <a href="/logout" class="bg-red-600 rounded-md inline-block ml-7 px-3 py-2 text-white">Logout</a>

        <div class="px-3 py-5 ml-5">
            <h5 class="mb-5 text-white">Edit Post</h5>
            <form method="post" action="/update/<%= post._id %>">
                <textarea placeholder="What's on your mind?" class="p-3 w-1/3 text-white resize-none bg-transparent border-2 border-zinc-600 rounded-md" name="content"><%= post.content %></textarea>
                <input class="px-3 py-2 bg-yellow-500 tex-black block rounded-md mt-2 text-s" type="submit" value="Update Post">
            </form>
        </div>
        
    </div>
    
</body>
</html>